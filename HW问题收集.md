## HW 问题收集

列举在HW 1、HW1.1过程里，你所遇到的2\~3个通过自己学习已经解决的问题，和2\~3个尚未解决的问题与挑战

### 已解决

1. 为什么修改领域对象内部数据后，Svelte 界面不自动刷新？
   
   1. **上下文**：在 HW1.1 接入 Svelte 时，调用 game.guess() 修改了内部 grid，但界面没有更新。
   2. **解决手段**：学习 Svelte 响应式原理，理解必须通过 store.set() 触发更新，于是在 gameStore 中加入 sync() 方法统一同步状态，解决了界面不刷新问题。

2. Game 历史记录 Undo/Redo 失效，状态互相污染
   
   1. **上下文**：HW1 最初版本中，Undo 后棋盘状态错乱，发现历史记录和当前状态互相影响。
   2. **解决手段**：学习深拷贝与浅拷贝区别，把所有 clone()、getGrid() 都改为深拷贝，确保历史快照与当前状态完全独立，最终修复 Undo/Redo 逻辑。

### 未解决

1. 原有项目的 store 与我自己写的领域对象如何完全融合？

   1. **上下文**：项目原本有 grid、userGrid、invalidCells 等内部 store，我接入自己的 Sudoku/Game 后，不知道如何彻底替换原有状态，只能部分混用。
   2. **尝试解决手段**：尝试替换但担心破坏原有 UI 逻辑，没有找到完美的完全解耦方式。

2. 如何让领域对象与原有游戏的校验、提示、胜利判断自动联动？

   1. **上下文**：我已经接入了 guess、undo、redo，但数独的错误检查、完成判断仍然依赖原有逻辑，没有完全接入我的 Sudoku 对象。
   2. **尝试解决手段**：尝试把校验逻辑写入 Sudoku 类，但与现有 UI 状态整合困难，暂时未完全打通。
